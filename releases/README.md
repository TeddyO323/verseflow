Place local release artifacts here before uploading them to GitHub Releases.

This folder is intentionally kept in the repo, but generated binaries such as
`.apk`, `.dmg`, `.pkg`, and similar installer files are ignored by Git.

Current expected artifacts:
- `VerseFlow-android-phone-debug.apk`
- `VerseFlow-android-auto-debug.apk`
- `VerseFlow-mac.dmg`

Upload those files to a GitHub Release instead of committing them to the
repository history.
